# Generate real credentials with: python3 deploy/init_env.py
POSTGRES_PASSWORD=
APP_DB_PASSWORD=
# Private acceptance over an SSH tunnel; no public listener.
SITE_URL=https://localhost:8443
BIND_ADDRESS=127.0.0.1
HTTP_PORT=8080
HTTPS_PORT=8443
CADDY_CONFIG=./Caddyfile.private
# Leave worker stopped until real SMTP credentials are configured.
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
