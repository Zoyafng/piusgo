'use client';
import Link from 'next/link';
import {useEffect,useState} from 'react';
import {type Catalog,money} from '@/lib/api';
import {MagnifyingGlass,UploadSimple,Star,ArrowRight} from './icons';
function Countdown({end}:{end:number}) {
  const [seconds,setSeconds]=useState<number|null>(null);
  useEffect(()=>{const tick=()=>setSeconds(Math.max(0,end-Math.floor(Date.now()/1000)));tick();const timer=setInterval(tick,1000);return ()=>clearInterval(timer);},[end]);
  return <strong className="countdown">{seconds===null?'--:--:--':[Math.floor(seconds/3600),Math.floor(seconds/60)%60,seconds%60].map(x=>String(x).padStart(2,'0')).join(':')}</strong>;
}
export default function Store({catalog}:{catalog:Catalog}) {
  const [category,setCategory]=useState(0),[query,setQuery]=useState(''),[share,setShare]=useState('');
  const items=catalog.items.filter(p=>(category===0||p.category===category)&&p.name.toLowerCase().includes(query.toLowerCase()));

  const selected=catalog.categories.find(c=>c.id===category)?.name||'热销';
  async function shareStore(){try{await navigator.clipboard.writeText(location.origin);setShare('店铺链接已复制');}catch{setShare(`店铺链接：${location.origin}`);}}
  return <main id="main" className="site-main home-main"><section className="home-heading"><div><span className="eyebrow">DIGITAL SERVICES</span><h1>选择你需要的数字服务</h1><p>价格、库存与交付状态实时展示，点击商品即可进入详情和下单页面。</p></div><div className="heading-actions"><label className="search-field"><MagnifyingGlass size={19}/><input aria-label="搜索商品名称" value={query} onChange={e=>setQuery(e.target.value)} placeholder="搜索商品名称" type="search"/></label><button className="secondary share" onClick={shareStore}><UploadSimple size={18}/>分享店铺</button></div></section>{share?<p className="notice" role="status">{share}<button onClick={()=>setShare('')}>关闭</button></p>:null}<div className="catalog-layout"><aside className="category-panel"><header><span className="eyebrow">CATEGORIES</span><h2>商品分类</h2></header><nav className="category-list" aria-label="商品分类"><button className={`category-item ${category===0?'active':''}`} onClick={()=>setCategory(0)}><i className="category-hot"><Star size={20}/></i><span>热销</span><b>{catalog.items.length}</b></button>{catalog.categories.map(c=><button key={c.id} className={`category-item ${category===c.id?'active':''}`} onClick={()=>setCategory(c.id)}><img src={c.image} alt="" width={34} height={34}/><span>{c.name}</span><b>{catalog.items.filter(p=>p.category===c.id).length}</b></button>)}</nav></aside><section className="catalog-products"><header className="section-heading"><div><span className="eyebrow">STORE</span><h2>{selected}</h2></div><span aria-live="polite">{items.length} 件商品</span></header><div className="product-grid">{items.map(p=><Link className={`product-card ${p.product_type==='lottery'?'lottery-card':''}`} href={`/products/${p.id}`} key={p.id}><div className="product-top"><img src={p.image} alt="" width={58} height={58}/><span className="badge">{p.stock===0?'已售罄':p.product_type==='lottery'?'抽奖商品':p.badge||'促销商品'}</span></div><h3>{p.name}</h3><div className="product-tags">{p.tags.map((tag,i)=><span className={`tone-${i%3}`} key={tag}>{tag}</span>)}</div>{p.product_type==='lottery'?<><p className="lottery-market">奖品价值 {money(p.price)}{(p.variant_count||1)>1?' 起':''}</p><div className="lottery-count"><span>已有 <b>{p.lottery_participants||0}</b> 人参与</span><span>每日 1 次</span></div><div className="lottery-bottom"><div><small>本期活动倒计时</small><Countdown end={catalog.lottery_ends_at}/></div><span className="lottery-action">{p.stock?'免费抽奖':'奖品已发完'} <ArrowRight size={14}/></span></div></>:<div className="product-bottom"><span className="product-price"><small>到手价</small><strong>{money(p.price)}{(p.variant_count||1)>1?<small> 起</small>:null}</strong></span><span className="stock">{p.stock===0?'暂时缺货':p.stock>=999?'库存充足':`库存 ${p.stock}`}</span></div>}</Link>)}</div>{items.length===0?<div className="empty"><MagnifyingGlass size={35}/><h3>没有找到相关商品</h3><p>换个关键词，或查看其他分类。</p><button className="secondary" onClick={()=>{setQuery('');setCategory(0);}}>查看全部商品</button></div>:null}</section></div></main>;
}
